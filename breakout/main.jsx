import React from 'react';
import ReactDOM from 'react-dom/client';
import BreakoutGame from './BreakoutGame.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BreakoutGame />
  </React.StrictMode>
);
